{% extends 'base.html.twig' %}

{% block body %}
    <h1>Gelato</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <td>
                    <img class="gelato-image" src="{{ asset(gelatoImage.webPath) }}" alt="{{ gelatoImage.path }}" />
                </td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('gelatoimage') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('gelatoimage_edit', { 'id': gelatoImage.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
