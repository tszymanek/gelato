{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            <h1>Gelato Image upload</h1>
        </div>
        <div class="row">
        {{ form_start(form) }}
            {{ form_row(form.files) }}
            <input type="submit" value="Upload" />
        {{ form_end(form) }}
        </div>
        <div class="row">
            <ul class="record_actions">
                <li>
                    <a href="{{ path('gelatoimage') }}">
                        Back to the list
                    </a>
                </li>
            </ul>
        </div>
    </div>
{% endblock %}
