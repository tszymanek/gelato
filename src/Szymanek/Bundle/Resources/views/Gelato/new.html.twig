{% extends 'base.html.twig' %}

{% block body %}
    <h1>Gelato creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul class="record_actions">
        <li>
            <a href="{{ path('gelato') }}">
                Back to the list
            </a>
        </li>
    </ul>
{% endblock %}
